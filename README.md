# Lab 5 - Starter
Make sure you make a PR to your own repo's main and not the class' repo!! Otherwise you will lose points!!

Humza Shaikh

1. I think that I wouldn't try to test the message feature with a unit test cause also the message features seems like one small components it's actually the combination of multiple components coming together to actaully be able to send the message. Like there is the test box or the validation and then also if the actual sending to the other user. So I think unit testing the messaging feature doesn't make sense to me. 

2. Yes, I would use a unit test to test the "max message length" feature because it is a small, isolated rule with a clear expected result. So for that small very specific component which doesn't have a big flow or multiple pieces working unit testing makes sense. 
